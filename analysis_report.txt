分析报告：scenarios/exp_macro_matrix.py

1. 当前的导入语句
   - 从 core.network 导入 GanacheManager, ConnectionManager (第41行)
   - 需要改为导入 AnvilManager

2. kill_ganache 函数的定义和使用位置
   - 定义位置：第57-66行，函数 kill_ganache()
   - 使用位置：
     * 第218行：预启动清理
     * 第249行：端口被占用时的重试循环
     * 第374行：迭代结束后的清理
   - 函数内容：使用 subprocess.call 杀死 ganache 进程（Windows 用 taskkill，Linux 用 pkill -f "ganache"）

3. 主进程生成循环中的时间延迟逻辑
   - 进程启动循环：第288-317行
   - 当前延迟：在第317行有 `time.sleep(2.0)` 注释为“反共振”
   - 这是进程启动之间的 2.0 秒延迟，符合阶段2要求（确保进程启动之间有 2.0 秒的延迟）

4. 需要修改的部分以符合阶段2的要求
   a) 导入更改：将 GanacheManager 改为 AnvilManager
   b) 函数替换：实现 kill_nodes 函数替换 kill_ganache
   c) 端口释放检查循环：需要添加对端口 [8545, 8546, 8547, 9545] 的释放检查
   d) 确保进程启动延迟：已存在 2.0 秒延迟，但需要确认是否足够

5. 其他相关代码
   - GanacheManager 实例化：第237行 `ganache = GanacheManager()`
   - start_network 调用：第243行 `ganache.start_network(topology)`
   - stop_network 调用：第373行 `ganache.stop_network()`

修改计划：
1. 更新导入语句：将 `from core.network import GanacheManager, ConnectionManager` 改为 `from core.network import AnvilManager, ConnectionManager`
2. 重命名 kill_ganache 函数为 kill_nodes，并调整实现以杀死 anvil 进程（使用 pkill -f "anvil" 或 taskkill /IM anvil.exe）
3. 更新所有调用点：
   - 第218行：kill_ganache() -> kill_nodes()
   - 第249行：kill_ganache() -> kill_nodes()
   - 第374行：kill_ganache() -> kill_nodes()
4. 添加端口释放检查函数 wait_for_ports_released(ports=[8545,8546,8547,9545])，在 kill_nodes 后调用，确保端口可用
5. 验证进程启动延迟：保持现有的 2.0 秒延迟
6. 将 GanacheManager 实例改为 AnvilManager：第237行 `ganache = AnvilManager()`
7. 注意：AnvilManager 的 stop_network 方法可能已正确停止进程，但 kill_nodes 仍需要作为后备。

需要检查 core/network.py 中 AnvilManager 的实现，确保其方法签名与 GanacheManager 兼容（start_network, stop_network）。